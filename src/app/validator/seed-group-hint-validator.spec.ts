import { SeedGroupHintValidator } from './seed-group-hint-validator';

describe('SeedGroupHintValidator', () => {
  it('should create an instance', () => {
    expect(new SeedGroupHintValidator()).toBeTruthy();
  });
});
